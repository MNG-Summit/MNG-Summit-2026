.DS_Store
._*
.claude/
proposal/
*.zip

# Spreadsheets are never published — Netlify serves the repo root, so a
# committed .xlsx would be downloadable at mngsummit.org/<name>.xlsx.
*.xlsx
_to_delete/

# Preview renders saved by the desktop app — never published
Claude outputs/
